import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
const SUPABASE_URL='https://vtrfgckzpjgtmqsnumur.supabase.co';
const SUPABASE_KEY='sb_publishable_zsgA314WZue1tlu_Kt-SDQ_UopdKMNs';
const db=createClient(SUPABASE_URL,SUPABASE_KEY);const $=s=>document.querySelector(s);const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));let user=null,campaign=null;
function notice(text,bad=false){const n=$('#notice');n.textContent=text;n.className=bad?'notice bad':'notice'}function show(id){document.querySelectorAll('[data-view]').forEach(x=>x.hidden=true);$(id).hidden=false}
async function refresh(){const {data:{user:u}}=await db.auth.getUser();user=u;$('#who').textContent=u?.email||'Private Alpha';$('#signout').hidden=!u;if(!u){show('#auth');return}await loadCampaigns();show('#dashboard')}
async function loadCampaigns(){const {data,error}=await db.from('campaigns').select('*').order('created_at',{ascending:false});if(error)return notice(error.message,true);$('#campaigns').innerHTML=(data||[]).map(c=>`<button class="campaign" data-id="${c.id}"><b>${esc(c.name)}</b><span>${esc(c.world_state?.genre||'Uncharted world')}</span></button>`).join('')||'<p>No campaigns yet. Create your first world.</p>';document.querySelectorAll('.campaign').forEach(b=>b.onclick=()=>openCampaign(b.dataset.id))}
async function openCampaign(id){const {data:c,error}=await db.from('campaigns').select('*').eq('id',id).single();if(error)return notice(error.message,true);campaign=c;const [{data:entities},{data:turns},{data:memory},{data:quests},{data:factions},{data:checks}]=await Promise.all([db.from('campaign_entities').select('*').eq('campaign_id',id).order('created_at'),db.from('game_turns').select('*').eq('campaign_id',id).order('created_at').limit(100),db.from('campaign_memory').select('*').eq('campaign_id',id).order('created_at',{ascending:false}).limit(16),db.from('campaign_quests').select('*').eq('campaign_id',id).eq('status','OPEN').order('created_at',{ascending:false}).limit(12),db.from('faction_relations').select('*').eq('campaign_id',id).order('score',{ascending:false}).limit(12),db.from('game_rule_checks').select('*').eq('campaign_id',id).order('created_at',{ascending:false}).limit(10)]);$('#campaignTitle').textContent=c.name;$('#world').textContent=JSON.stringify(c.world_state||{},null,2);$('#entities').innerHTML=(entities||[]).map(e=>`<div class="chip"><b>${esc(e.entity_type)}</b> ${esc(e.name)} <span class="muted">${esc(e.state?.archetype||'')}</span></div>`).join('')||'<p>No character yet.</p>';$('#transcript').innerHTML=(turns||[]).map(t=>`<article class="turn ${t.speaker.toLowerCase()}"><small>${esc(t.speaker)}${t.metadata?.reasoner?` · ${esc(t.metadata.reasoner)}`:''}</small><p>${esc(t.content)}</p></article>`).join('')||'<p>Your story has not begun.</p>';$('#memory').innerHTML=(memory||[]).map(m=>`<div class="memory"><b>${esc(m.memory_type)}</b><span>${esc(m.content?.summary||m.content?.text||JSON.stringify(m.content))}</span></div>`).join('')||'<p>No memories yet.</p>';$('#quests').innerHTML=(quests||[]).map(q=>`<div class="chip"><b>${esc(q.title)}</b><div class="muted">${esc(q.objective?.text||'Unresolved objective')}</div></div>`).join('')||'<p>No open quests.</p>';$('#factions').innerHTML=(factions||[]).map(f=>`<div class="chip"><b>${esc(f.faction_name)}</b> <span class="muted">relation ${f.score>=0?'+':''}${esc(f.score)}</span></div>`).join('')||'<p>No faction history yet.</p>';$('#checks').innerHTML=(checks||[]).map(r=>`<div class="chip"><b>${esc(r.skill)}</b> ${r.success?'✓':'✕'} ${r.total} vs DC ${r.dc} <span class="muted">d20 ${r.roll}${r.modifier?` ${r.modifier>0?'+':''}${r.modifier}`:''}</span></div>`).join('')||'<p>No checks recorded.</p>';show('#play');$('#action').focus()}
$('#authForm').onsubmit=async e=>{e.preventDefault();const email=$('#email').value.trim(),password=$('#password').value,mode=e.submitter.value;const r=mode==='signup'?await db.auth.signUp({email,password}):await db.auth.signInWithPassword({email,password});if(r.error)return notice(r.error.message,true);notice(mode==='signup'&&!r.data.session?'Check your email to confirm the account.':'Signed in.');await refresh()};$('#signout').onclick=async()=>{await db.auth.signOut();campaign=null;await refresh()};$('#newCampaign').onsubmit=async e=>{e.preventDefault();const name=$('#newName').value.trim(),genre=$('#genre').value.trim()||'pulp fantasy',location=$('#location').value.trim()||'the frontier gate';const {data,error}=await db.from('campaigns').insert({owner_id:user.id,name,world_state:{genre,location,clock:'Day 1',threat_level:1}}).select().single();if(error)return notice(error.message,true);$('#newCampaign').reset();await loadCampaigns();await openCampaign(data.id)};$('#characterForm').onsubmit=async e=>{e.preventDefault();if(!campaign)return;const name=$('#pcName').value.trim(),archetype=$('#archetype').value.trim()||'wanderer';const {error}=await db.from('campaign_entities').insert({campaign_id:campaign.id,entity_type:'PC',name,state:{archetype,status:'active'}});if(error)return notice(error.message,true);notice('Character created.');await openCampaign(campaign.id)};$('#turnForm').onsubmit=async e=>{e.preventDefault();if(!campaign)return;const action=$('#action').value.trim();if(!action)return;const client_request_id=crypto.randomUUID();$('#send').disabled=true;notice('GM Reasoner v1 is resolving the turn…');const {data,error}=await db.functions.invoke('gm-turn',{body:{campaign_id:campaign.id,action,client_request_id}});$('#send').disabled=false;if(error)return notice(error.message,true);$('#action').value='';const mode=data.reasoner==='model-v1'?'model':'deterministic fallback';notice(`Turn ${data.turn} recorded · ${mode}${data.check?` · ${data.check.success?'check passed':'check failed'}`:''}.`);await openCampaign(campaign.id)};$('#back').onclick=async()=>{campaign=null;await loadCampaigns();show('#dashboard')};db.auth.onAuthStateChange(()=>setTimeout(refresh,0));refresh();
