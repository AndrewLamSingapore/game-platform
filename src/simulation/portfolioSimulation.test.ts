import {runScenario,simulationEvent} from './portfolioSimulation'; const s={id:'x',seed:42,iterations:100,strategies:[{id:'a',parameters:{x:1}},{id:'b',parameters:{x:2}}],assumptions:['model only']}; const a=runScenario(s),b=runScenario(s); if(JSON.stringify(a)!==JSON.stringify(b)) throw new Error('simulation must be deterministic'); if((simulationEvent(a,'evt-12345678') as any).payload.real_world_proof!==false) throw new Error('evidence boundary regression');
