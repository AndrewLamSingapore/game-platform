import { deliverPortfolioOutbox } from '../server/services/portfolioEventClient.js';
export default async function handler(req,res){res.setHeader('Content-Type','application/json; charset=utf-8');res.setHeader('Cache-Control','no-store');if(req.method!=='GET'){res.setHeader('Allow','GET');return res.status(405).json({error:'method_not_allowed'});}const expected=process.env.CRON_SECRET||process.env.PORTFOLIO_OUTBOX_TOKEN;if(!expected)return res.status(503).json({error:'outbox_authorization_not_configured'});if(req.headers?.authorization!==`Bearer ${expected}`)return res.status(401).json({error:'unauthorized'});const result=await deliverPortfolioOutbox({limit:50});return res.status(result.reason?503:200).json({ok:!result.reason,...result});}
